# Optional Blueprint — LEGACY Render hosting (prefer Cloud Run).
# Keep for rollback until GCP cutover is complete, then delete.
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: craftguru-api
    runtime: node
    region: oregon
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
