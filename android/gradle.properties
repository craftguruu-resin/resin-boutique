org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Production storefront URL (same origin serves static + /api when deployed via Cloud Run).
CRAFTGURU_WEB_BASE_URL=https://www.craftguru.co.in

# Optional: override API base when static host differs from API (leave empty for same-origin).
CRAFTGURU_API_BASE_URL=
